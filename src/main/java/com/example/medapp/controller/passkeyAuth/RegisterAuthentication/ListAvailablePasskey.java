package com.example.medapp.controller.passkeyAuth.RegisterAuthentication;
/* 
import org.springframework.web.bind.annotation.RestController;

import com.example.medapp.repository.RegistrationStorage.InMemoryCredentialRepository;
import com.yubico.webauthn.RegisteredCredential;

import jakarta.persistence.criteria.CriteriaBuilder.In;

import java.util.Set;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

@RestController
public class ListAvailablePasskey {

    @Autowired
    InMemoryCredentialRepository inMemorycredentialRepository;

    @GetMapping("/getavailendpoint")
    public String getMethodName(@RequestParam String param) {
        return new String();
    }

    public Set<RegisteredCredential> listAvailablePasskey() {
        // This method should return a list of available passkeys
        // For now, we will return a placeholder string
        return inMemorycredentialRepository.lookupAll(null);
    }

} */